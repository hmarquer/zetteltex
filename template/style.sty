% style.sty — formatting and package customization.
% It is loaded automatically by the texnote/texbook classes. The functional
% engine (reference macros, hyperlinks, bibliography) lives in ztxbase.sty and
% should not be touched: edit this file to customize formatting and packages.

\usepackage[margin=2.5cm]{geometry} % page margins
\usepackage{xcolor} % colors

% Example formatting packages you can add here:
% \usepackage{parskip}
% \usepackage[spanish]{babel}  % added by `zetteltex init_config` when Spanish is selected
% \usepackage{titlesec}
% \usepackage{pgfplots}