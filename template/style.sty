% style.sty — personalización de formato y paquetes.
% Lo cargan automáticamente las clases texnote/texbook. El "motor" funcional
% (macros de referencia, hiperenlaces, bibliografía) vive en ztxbase.sty y no
% hace falta tocarlo: edita este archivo para personalizar formato y paquetes.

\usepackage[margin=2.5cm]{geometry} % márgenes de página
\usepackage{xcolor} % colores

% Ejemplos de paquetes de formato que puedes añadir aquí:
% \usepackage{parskip}
% \usepackage[spanish]{babel}  % lo añade `zetteltex init_config` al elegir es
% \usepackage{titlesec}
% \usepackage{pgfplots}